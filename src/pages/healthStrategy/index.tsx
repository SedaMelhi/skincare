import HealthStrategyPage from '@/components/screens/healthStrategy';
import { NextPage } from 'next';

const HealthStrategy: NextPage = () => {
  return <HealthStrategyPage />;
};

export default HealthStrategy;
